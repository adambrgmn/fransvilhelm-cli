import React from 'react';
import { Setup } from '../src/Setup';

/// Setup standard development environment
const SetupCommand = (props: {}) => <Setup {...props} />;

SetupCommand.propTypes = {};

SetupCommand.defaultProps = {};

export default SetupCommand;
